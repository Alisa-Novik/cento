# Factory Autopilot Runtime Fixtures

`complex-project` is generated by `scripts/factory_autopilot_runtime_e2e.py`.
It creates at least 50 tasks across coordinator, builder, validator, docs-evidence,
and integration-style work, then seeds validation, patch, and integration backlogs
for the dry-run control loop.
